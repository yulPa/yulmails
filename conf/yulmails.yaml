version: "1"

services:

  senders:
    - name: "sender1"
      host: "localhost"
      port: "12800"

  computes:
    - name: "compute1"
      host: "localhost"
      port: "25600"

  entrypoints:
    - name: "entry1"
      host: "localhost"
      port: "19200"
